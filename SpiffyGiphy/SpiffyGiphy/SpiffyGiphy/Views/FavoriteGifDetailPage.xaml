<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SpiffyGiphy.Views.FavoriteGifDetailPage"
             Title="{Binding Title}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <WebView Source="{Binding FavoriteGif.Url}" />

        <Label Text="{Binding FavoriteGif.Caption}"
               FontSize="Medium"
               Grid.Row="1" />
    </Grid>
</ContentPage>